# GitHub Pages branch — redirect only.
# The app itself lives on main and is deployed to https://go.soxylo.com
# (Cloudflare Pages: needs _headers COOP/COEP + the /models/* Function for the dan net,
#  neither of which GitHub Pages supports).
